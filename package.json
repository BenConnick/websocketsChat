{
  "name": "meowssenger",
  "version": "1.0.0",
  "description": "Messenger app using react",
  "main": "./src/chat-server.js",
  "scripts": {
    "nodemon": "nodemon --watch ./src ./src/server.js",
    "start": "node ./src/chat-server.js",
    "test": "echo \"No test specified\"",
    "auto": "git add -A && git commit -m 'auto commit' && git push && git push heroku"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apn": "^2.1.5",
    "express": "^4.15.2",
    "firebase-admin": "^5.12.0",
    "mongodb": "^2.2.31",
    "sjcl": "^1.0",
    "socket.io": "^1.7.2",
    "websocket": "^1.0.24"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
